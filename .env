# Copy this file to ".env" and fill in the real credentials before running the app.

# --- Server ---
PORT=8081

# --- Database ---
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DB=knoledge
MYSQL_USER=root
MYSQL_PASSWORD=

# --- JWT / Client ---
# Generate at least 256 bits of entropy, e.g.:
# openssl rand -base64 32
JWT_SECRET=replace-this-with-a-real-base64-secret
JWT_EXPIRATION_MINUTES=240
CLIENT_ORIGIN=http://localhost:5173
