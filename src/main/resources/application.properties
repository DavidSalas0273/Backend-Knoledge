# Load .env file for local development (optional - Spring Boot 3.x feature)
spring.config.import=optional:file:.env[.properties]

spring.application.name=Knoledge

# Puerto (Railway lo inyecta en PORT)
server.port=${PORT:8080}

# MySQL (Railway env: MYSQLHOST, MYSQLPORT, MYSQLUSER, MYSQLPASSWORD, MYSQLDATABASE)
spring.datasource.url=jdbc:mysql://${MYSQLHOST:${MYSQL_HOST:switchyard.proxy.rlwy.net}}:${MYSQLPORT:${MYSQL_PORT:30028}}/${MYSQLDATABASE:${MYSQL_DB:railway}}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:${MYSQL_USER:root}}
spring.datasource.password=${MYSQLPASSWORD:${MYSQL_PASSWORD:JmmqJHkohksIVsnPHMNeUupQjcLDmLmX}}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=true

# JWT
jwt.secret=${JWT_SECRET:dGhpc2lzYXRlc3RzZWNyZXR0aGF0aXN2ZXJ5bG9uZ2FuZHNob3VsZGJlYWVudHlzaXhhNGNoYXJhY3RlcnNsb25nYmFzZTY0ZW5jb2Rpbmdmb3JqYXQ=}
jwt.expiration-minutes=${JWT_EXPIRATION_MINUTES:240}

# CORS
client.origin=${CLIENT_ORIGIN:http://localhost:5173}
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,https://frontend-knoledge.vercel.app,https://backend-knoledge-production.up.railway.app}

# Archivos grandes
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB

# Eliminar warning de Spring Security
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR
